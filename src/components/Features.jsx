import { useState, useRef } from "react";
import { TiLocationArrow } from "react-icons/ti";

export const BentoTilt = ({ children, className = "" }) => {
  const [transformStyle, setTransformStyle] = useState("");
  const itemRef = useRef(null);

  const handleMouseMove = (event) => {
    if (!itemRef.current) return;

    const { left, top, width, height } =
      itemRef.current.getBoundingClientRect();

    const relativeX = (event.clientX - left) / width;
    const relativeY = (event.clientY - top) / height;

    const tiltX = (relativeY - 0.5) * 5;
    const tiltY = (relativeX - 0.5) * -5;

    const newTransform = `perspective(700px) rotateX(${tiltX}deg) rotateY(${tiltY}deg) scale3d(.95, .95, .95)`;
    setTransformStyle(newTransform);
  };

  const handleMouseLeave = () => {
    setTransformStyle("");
  };

  return (
    <div
      ref={itemRef}
      className={className}
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
      style={{ transform: transformStyle }}
    >
      {children}
    </div>
  );
};

export const BentoCard = ({ src, title, description, metrics }) => {
  return (
    <div className="relative size-full">
      <video
        src={src}
        loop
        muted
        autoPlay
        className="absolute left-0 top-0 size-full object-cover object-center"
      />
      <div className="relative z-10 flex size-full flex-col justify-between p-5 text-blue-50">
        <div>
          <h1 className="bento-title special-font">{title}</h1>
          {description && (
            <p className="mt-3 max-w-64 text-xs md:text-base">{description}</p>
          )}
        </div>
        {metrics && (
          <div className="mt-4">
            <p className="text-xs uppercase text-blue-100 opacity-80">{metrics}</p>
          </div>
        )}
      </div>
    </div>
  );
};

const Features = () => (
  <section className="bg-black pb-52">
    <div className="container mx-auto px-3 md:px-10">
      <div className="px-5 py-32">
        <p className="font-circular-web text-lg text-blue-50">
          Selected Projects
        </p>
        <p className="max-w-md font-circular-web text-lg text-blue-50 opacity-50">
          Production-ready systems built at the intersection of AI, full-stack development,
          and distributed architecture—delivering measurable impact across diverse domains.
        </p>
      </div>

      <BentoTilt className="border-hsla relative mb-7 h-96 w-full overflow-hidden rounded-md md:h-[65vh]">
        <BentoCard
          src="videos/feature-1.mp4"
          title={
            <>
              Grow Smart <b>A</b>I
            </>
          }
          description="AI-Powered Crop Intelligence Platform: Computer vision system for crop disease detection with 95% accuracy, serving 500+ farmers across 12 regions."
          metrics="500+ FARMERS • 95% ACCURACY • 12 REGIONS"
        />
      </BentoTilt>

      <div className="grid h-[135vh] w-full grid-cols-2 grid-rows-3 gap-7">
        <BentoTilt className="bento-tilt_1 row-span-1 md:col-span-1 md:row-span-2">
          <BentoCard
            src="videos/feature-2.mp4"
            title={
              <>
                A.R.J.U.N.<b>A</b>
              </>
            }
            description="Adaptive Learning & AI Tutoring System: NLP-powered adaptive learning platform with real-time feedback and personalized curriculum, improving student outcomes by 40%."
            metrics="1000+ STUDENTS • 5K+ SESSIONS • 40% IMPROVEMENT"
          />
        </BentoTilt>

        <BentoTilt className="bento-tilt_1 row-span-1 ms-32 md:col-span-1 md:ms-0">
          <BentoCard
            src="videos/feature-3.mp4"
            title={
              <>
                Prep<b>W</b>ise
              </>
            }
            description="Unified Interview Preparation Platform: Comprehensive coding practice and mock interview platform with progress analytics, achieving 78% success rate."
            metrics="300+ PROBLEMS • 2K+ USERS • 78% SUCCESS RATE"
          />
        </BentoTilt>

        <BentoTilt className="bento-tilt_1 me-14 md:col-span-1 md:me-0">
          <BentoCard
            src="videos/feature-4.mp4"
            title={
              <>
                Continuous Chaos <b>P</b>revention
              </>
            }
            description="AI-Driven Production Debugging Tool: Automated root cause analysis using distributed tracing and ML, reducing debugging time by 80%."
            metrics="50K+ ISSUES ANALYZED • 80% TIME REDUCTION • 92% ACCURACY"
          />
        </BentoTilt>

        <BentoTilt className="bento-tilt_2">
          <div className="flex size-full flex-col justify-between bg-violet-300 p-5">
            <h1 className="bento-title special-font max-w-64 text-black">
              View All <b>P</b>rojects
            </h1>

            <TiLocationArrow className="m-5 scale-[5] self-end" />
          </div>
        </BentoTilt>

        <BentoTilt className="bento-tilt_2">
          <video
            src="videos/feature-5.mp4"
            loop
            muted
            autoPlay
            className="size-full object-cover object-center"
          />
        </BentoTilt>
      </div>
    </div>
  </section>
);

export default Features;
